# Agent-ready contract — core

> 🚧 This page will define the agent-ready contract for `@tetherto/mdk-core` exports: when an export needs `USAGE.md` and an `examples/` entry (QVAC model), public-export rules for `mdk`, `client`, `kernel`, `gateway`, `lib-stats`, `mock-control-service`, and how core surfaces its agent-ready metadata (TBD: JSDoc tags in line with the UI side, or a per-package contract file).
>
> Cross-workspace SDK rules: [`docs/reference/maintainers/agent-ready-sdk.md`](../../../docs/reference/maintainers/agent-ready-sdk.md).

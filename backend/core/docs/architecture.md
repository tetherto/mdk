# Core architecture

> 🚧 This page will describe the core workspace packages: `mdk`, `client`, `kernel`, `gateway`, `lib-stats`, `mock-control-service`, and how they compose the orchestration kernel and SDK surface.

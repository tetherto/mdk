# Agent-ready contract — workers

> Coming soon. This page will explain the worker-side contract using the existing `mdk-contract.json`: how `metadata`, `capabilities.telemetry`, `commands`, `health`, and `errors` are read by the docs catalogue, plus the `USAGE.md` + `examples/<model>.start.sh` (one per `--wtype`) conventions added on top.
>
> Cross-workspace contract rules: [`docs/reference/maintainers/agent-ready-sdk.md`](../../../docs/reference/maintainers/agent-ready-sdk.md).

# Workers architecture

> Coming soon. This page will describe what a worker is, lifecycle, the `--wtype` convention, and the taxonomy: `base`, `miners/`, `containers/`, `minerpools/`, `power-meter/`, `temperature/`.

{
  "name": "@tetherto/mdk-site-monitor-example",
  "version": "0.5.0",
  "private": true,
  "description": "MDK end-to-end example — mock worker, Kernel, gateway, and site hashrate UI",
  "license": "Apache-2.0",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "postinstall": "npm install --prefix ./ui",
    "start": "node start.js"
  }
}

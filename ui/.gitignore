# Dependencies
node_modules

# WIP: generated Typedoc API reference (regen and track later)
api/

# Build outputs
dist
dist-css
.next
out
build

# Test coverage
coverage
*.lcov

# Compiled CSS (should be in dist, not src)
packages/*/src/styles.css
packages/*/src/styles.css.map

# Turbo
.turbo

# Environment
.env
.env.*
!.env.example

# IDE
.idea
.vscode
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Logs
*.log
npm-debug.log*

# Cache
.cache
*.tsbuildinfo

# Starter bootstrap app (local scaffold, not for publishing)
apps/starter

# Example app generated by the CLI from packages/cli/templates/mdk-ui-shell
# (the template is the source of truth). Regenerate with `npm run generate:shell`.
apps/mdk-ui-shell

# Local mdk-ui CLI probe scaffolds (developer-only, not for publishing)
tmp-mdk-check-*/
